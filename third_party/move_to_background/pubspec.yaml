name: move_to_background
description: Local patched fork of move_to_background with AGP namespace support.
version: 1.0.2+local
homepage: https://example.invalid/momentum-habits

environment:
  sdk: '>=3.3.0 <4.0.0'
  flutter: '>=3.19.0'

dependencies:
  flutter:
    sdk: flutter

flutter:
  plugin:
    platforms:
      android:
        package: com.github.riod.move_to_background
        pluginClass: MoveToBackgroundPlugin
